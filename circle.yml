machine:
  timezone:
    Asia/Tokyo
  node:
    version: 6.10.3

dependencies:
  override:
    - npm install --only=dev

test:
  override:
    - echo "Nothing to do here"

deployment:
  production:
    branch: master
    commands:
      - ./deploy.sh