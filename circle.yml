machine:
  timezone:
    Asia/Tokyo
#  node:
#    version: 6.10.3

dependencies:
  override:
    #- npm install --only=dev
    - echo "Nothing to do here"

test:
  override:
    - echo "Nothing to do here"

